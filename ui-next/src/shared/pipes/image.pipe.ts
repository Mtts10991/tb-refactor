/**
 * @fileoverview
 * Stub ชั่วครางของ image pipe — จะถูก port ใน Phase 2 (shared library)
 *
 * @parityEngine Angular
 * พอร์ตมาจาก: ui-ngx/src/app/shared/pipe/image.pipe.ts
 */

export function imagePipe(input: string): string {
  return input;
}
